# QA artifacts & secrets (tripo key, screenshots)
.qa/
# image-gen working dir
_gen/
# local backup chain (git history replaces these)
index-v*-cn.html
# OS / editor junk
.DS_Store
Thumbs.db

# third-party LPC asset pack (36MB, embedded git repo)
vendor/

# local LLM default config (contains API key — never commit)
multius.local.js

# raw vlog/demo footage (9GB, never deploy/commit)
media/
